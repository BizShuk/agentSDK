# 規格摘要 (Specs Summary) — 2026-08-04

整併自 `docs/specs/` 中 21 份文件（2026-07-04 ~ 2026-07-18），
涵蓋 2026-07-21 之前的設計歷史。本檔吸收前一份 `2026-07-29-Summary.md` 的全部列。
現行邊界與 ownership 仍以 [`CLAUDE.md`](../../CLAUDE.md) 為準。

| 日期 (Date) | 功能 (Feature) | 使用方式 (How to Use) | 價值 (Value) |
| ----------- | -------------- | --------------------- | ------------ |
| 2026-07-04 | HITL 與 provider 解耦：`agent/permission`、`middleware/security`、`provider/` | `eng.Middleware = preset.Secure(tool.DefaultPolicy(), permission.DefaultApprovalPolicy{})` | 讓同一個 runtime 可抽換模型 provider，並在高風險工具執行前可靠暫停與取得人工核准。 |
| 2026-07-04 | Agent loop 核心骨架：`core.Decide` + `runtime.Engine` | `eng := runtime.NewEngine(decide, model, tools)` | 以純 transition contract 隔離 I/O，使 agent loop 可測試、可重播且可由不同應用組裝。 |
| 2026-07-04 | Crash recovery 與循環防禦：`memory/filestore` + `middleware/preset.Default` | 設定 `eng.Middleware = preset.Default()`，再以 `eng.Resume(ctx, runID)` 復原 | 透過 snapshot、WAL、retry、timeout、budget 與 loopguard，避免崩潰後重做模型或工具副作用。 |
| 2026-07-04 | 工具安全鏈：`tool.SchemaForTool` + `middleware/security` + `middleware/observability` | `eng.Middleware = preset.Secure(tool.DefaultPolicy(), approval)` | 把 schema 驗證、sandbox、prompt-injection 防護與 tracing 從 runtime dispatch 邏輯中分離。 |
| 2026-07-07 | Typed tool 與 registry：`tool.RegisterFunc`、`tool.Registry` | `tool.RegisterFunc(reg, "name", "description", risk, fn)` | 收斂 JSON 轉換、schema 反射與工具 dispatch 樣板，讓應用只需提供 typed business function。 |
| 2026-07-07 | CLI process host：`agent/cli.OpenForCLI` | `host := agentcli.MustOpenForCLI("my-agent", slog.LevelInfo)` | 一次建立標準資料、WAL 與 log 路徑，並讓空 app name 在啟動期 fail fast。 |
| 2026-07-07 | Headless JSONL wire：`agent/wire.Envelope`、`wire.Sink` | `agent.New(cfg, agent.WithSink(wire.NewSink(os.Stdout)))` | 讓 CLI、dashboard 與外部 process 以穩定 JSONL 觀察 agent stream，而不依賴內部 state shape。 |
| 2026-07-07 | 純狀態機資料契約：`core.State`、`core.Event`、`core.Instruction`、`core.Decide` | `next, instructions := decide(state, event)` | 以 stdlib-only、可序列化的資料模型保證 transition 可預測、可持久化與可重播。 |
| 2026-07-07 | 六個內建工具：`tool/builtin` 的 Read / Write / Edit / Bash / Glob / Grep | `builtin.RegisterDefaults(reg, builtin.Options{Policy: tool.DefaultPolicy()})` | 提供 bounded、sandbox-aware 的常用檔案與 shell 能力，免除每個 agent 重寫基礎工具。 |
| 2026-07-07 | Context window 與 durable state：`memory.Window`、`memory.Compactor`、`checkpoint.Recoverer` | `checkpoint.NewRecoverer(store, wal).Recover(ctx, runID)` | 在 token 上限內保留對話，並以 snapshot + WAL 在不重發模型呼叫的前提下復原 run。 |
| 2026-07-07 | 可組合 middleware chain：`middleware.Chain`、`preset.Default`、`preset.Secure` | `eng.Middleware = preset.Secure(sandbox, approval)` | 將 retry、timeout、budget、loopguard、安全與觀測政策組成可獨立測試的 ordered chain。 |
| 2026-07-07 | 六種 reasoning FSM：`reasoning.NewRule`、`reasoning.NewDecide` | `rule, err := reasoning.NewRule(core.REASON_REACT)` | 讓應用以同一個純函式 contract 選擇 ReAct、planner、review、one-shot、reflection 或 router 策略。 |
| 2026-07-07 | Provider adapters 與 registry：`provider.New`、`provider.Entry` | `go run . provider --list-providers` | 把 vendor wire format、認證與 model catalog 留在 adapter layer，維持 runtime 的最小 provider port。 |
| 2026-07-07 | Runtime shell：`runtime.Engine` | `eng := runtime.NewEngine(decide, provider, registry)` | 集中 dispatch model、tool、approval、notify 與 persistence ports，同時保持 reasoning transition 無 I/O。 |
| 2026-07-07 | 持續 log 診斷 sample：`sample/logdoctor-agent` | `cd sample/logdoctor-agent && MINIMAX_API_KEY=... go run . watch` | 以可執行 sample 驗證 log discovery、單輪診斷、stream events 與成功後才提交 cursor 的完整流程。 |
| 2026-07-07 | Deterministic test doubles：`utils/testutil` | `provider := testutil.NewScriptedProvider(); store := testutil.NewMemStore()` | 讓 runtime、agent 與 sample 測試在無網路環境下精確控制模型回應、state、WAL 與通知。 |
| 2026-07-11 | Built-in tool set in `tool/builtin` | `builtin.RegisterDefaults(reg, builtin.Options{Policy: tool.DefaultPolicy()})` | Gives agents six bounded filesystem and shell tools without requiring every application to rebuild the same tool boilerplate. |
| 2026-07-13 | 補完 reasoning strategies：`OneShotReasoning`、`LearnFromFailure`、`ChooseAgent` | `reasoning.NewRule(core.REASON_ONE_SHOT)`、`reasoning.NewRule(core.REASON_LEARN_FROM_FAILURE)` 或 `reasoning.NewRule(core.REASON_PICK_AGENT)` | 將原本的 placeholder 補成可抵達終態的純函式 FSM，避免 retry 或 WAL replay 重發模型呼叫。 |
| 2026-07-18 | MiniMax provider adapter：`provider/minimax` | `go run . provider "ping" --provider minimax`（讀 `MINIMAX_API_KEY`） | 讓 MiniMax 的 identity、environment 與 default model 獨立成 adapter，不複製 Anthropic Messages 的 request/response translation。 |
| 2026-07-18 | Durable cursor 與 incremental tail：`sample/logdoctor-agent/core` 的 `tail.go`、`checkpoint.go` | `cd sample/logdoctor-agent && go run . watch` | 只在 batch 含新完整行時才呼叫 LLM，並正確處理 partial line、rename rotation 與 copytruncate。 |
| 2026-07-18 | Anthropic adapter correctness 修正：`ROLE_SYSTEM` → top-level `system`、`ToolSpec.Parameters` → `input_schema` root | adapter 內建，無需設定；見 `provider/anthropic/translate.go` 與 `dto.go` | 避免 system prompt 被當成 user message，以及 tool schema 多包一層導致 provider 無法解析。 |

## 已淘汰 (Deprecated)

- `mcp.Client`：`mcp/` 已於 `f1eecd0` 移除；目前只保留未接 production consumer 的 `tool.ToolSource` 介面。
- `perception.Multi` / `perception.Normalizer`：`perception/` 已於 `a23f41e` 移除；觀察輸入只保留最小的 `core.ObservationSource` port。
- Logdoctor proposal / approval lifecycle（immutable proposal + digest、`list` / `show` / `approve` / `reject` 子指令）：已於 `879e246` 隨 log-agent-v2 重構移除；`sample/logdoctor-agent` 現只保留 `analyze` 與 `watch`。
- `provider/openaicompat`：已於 `551410d` 移除；OpenAI-compatible wire 改由 `provider/protocol/openaichat` 共用 codec 承接。

## 來源檔案 (Source Files)

本次吸收：

- `2026-07-18-continuous-logdoctor-minimax.md`
- `2026-07-29-Summary.md`（前一份摘要，其來源如下）

前一份摘要的來源：

- `2026-07-04-architecture-decoupling-hitl-and-providers.md`
- `2026-07-04-core-paradigm-and-sample-skeleton.md`
- `2026-07-04-system-resilience-and-loop-defense.md`
- `2026-07-04-tool-ecosystem-and-runtime-security.md`
- `2026-07-07-action-tool-pillar.md`
- `2026-07-07-appname-wiring-helper.md`
- `2026-07-07-cli-envelope-codec.md`
- `2026-07-07-core-state-machine.md`
- `2026-07-07-m5-built-in-tools.md`
- `2026-07-07-mcp-client.md`
- `2026-07-07-memory-pillar.md`
- `2026-07-07-middleware-chain.md`
- `2026-07-07-perception-input-pillar.md`
- `2026-07-07-planning-thinking-patterns.md`
- `2026-07-07-provider-implementations.md`
- `2026-07-07-runtime-shell.md`
- `2026-07-07-sample-logdoctor.md`
- `2026-07-07-test-utilities.md`
- `2026-07-13-complete-stub-thinking-patterns.md`
- `ancient-cuddling-quilt.md`（文件日期以首次 Git 提交判定為 2026-07-11）
