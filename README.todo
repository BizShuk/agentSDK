# README.todo — agentsdk 進行中項目索引

> 進行中計畫的 single index。每個項目對應一份 `plans/YYYY-MM-DD-<slug>.md` 細節檔。
> 項目完成(對應 milestone ✅)後,規格轉入 `docs/specs/YYYY-MM-DD-<slug>.md`,本檔條目改 ✅ 並保留連結當歷史索引。

最後更新: 2026-07-07

## Milestone 狀態

| ID     | 範疇                          | 狀態      | 細節                                                                                 |
| ------ | ----------------------------- | --------- | ------------------------------------------------------------------------------------ |
| M1     | 核心範式 + sample 骨架        | ✅        | (歷史)已收,規格已轉 `docs/specs/`                                                    |
| M2     | 系統韌性 + 循環防禦           | ✅        | (歷史)已收,規格已轉 `docs/specs/`                                                    |
| **M3** | 工具生態 + 執行期安全         | ⏳ 進行中 | [`plans/2026-07-07-m3-tooling-security.md`](plans/2026-07-07-m3-tooling-security.md) |
| **M4** | 架構解耦 + HITL + 三 provider | ⏳ 排隊中 | [`plans/2026-07-07-m4-hitl-providers.md`](plans/2026-07-07-m4-hitl-providers.md)     |

## 進行中項目 (Active)

- [ ] M3 — 工具生態 + 執行期安全
    - **檔案**: `plans/2026-07-07-m3-tooling-security.md`
    - **範疇**: `action/schema.go` (invopop/jsonschema 反射) / `action/sandbox.go` / `middleware/security/{spotlight,sanitizer,sandbox_mw}.go` / `mcp/` (modelcontextprotocol/go-sdk) / sample logdoctor 擴 todo tools
    - **carry-over**: 決定 `perception/` 去留(wire / inline / delete)
    - **Pre-flight 未過項目**: 確認 `invopop/jsonschema` 與 `modelcontextprotocol/go-sdk` 在 module cache;`go.work` 加 `./mcp`;`.golangci.yml` 加 `ST1003` 例外
    - **驗收**: 見 M3 細節檔「驗收」段

- [ ] M4 — 架構解耦 + HITL + 三 Provider
    - **檔案**: `plans/2026-07-07-m4-hitl-providers.md`
    - **範疇**: `action/approval_policy.go` (L1/L2 企業預設) / `cli/{envelope,codec}.go` / `provider/{anthropic,openaicompat,google}/` / sample logdoctor 擴 watch/approve CLI
    - **前置**: M3 全部 DoD 過
        - **驗收**: 含 mid-run approval State round-trip / 三 provider 實測 / 完整 watch→fix e2e

## 跨項目慣例提醒

- **命名**: 計畫檔 `plans/YYYY-MM-DD-<slug>.md`;規格檔 `docs/specs/YYYY-MM-DD-<slug>.md`
- **DoD 共通**: `go test ./...` 全綠 + `golangci-lint run` 全綠 + `CLAUDE.md` Milestone 表狀態更新 + 本檔條目更新
- **風險表**: 每個計畫檔有自己的 `## 風險` 段,Pre-flight 段把 module cache 與 toolchain 確認列在最前

## 變更紀錄

- 2026-07-07: 初版,索引 M3 + M4。`plans/2026-07-07-m3-tooling-security.md` 與 `plans/2026-07-07-m4-hitl-providers.md` 同步建立。
