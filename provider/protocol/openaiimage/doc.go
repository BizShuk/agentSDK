// Package openaiimage encodes and decodes the shared OpenAI-compatible
// /images/generations wire contract. It owns JSON shape and bounded response
// handling, not provider selection, credential lookup, or endpoint defaults.
package openaiimage
